server:
  port: 8085
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/josesuri/BC01P01-CONFIG-DATA
          default-label: main
  application:
    name: config-server